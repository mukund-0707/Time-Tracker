<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Time Tracker</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    
    <style>
        /* Full-screen background */
        body {
            background-image: url('https://t3.ftcdn.net/jpg/02/77/41/26/240_F_277412640_WVE1FmtHzgIiVCMTrTkvZC13dNzLD04Z.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        /* Semi-transparent overlay for better readability */
        .overlay {
            background-color: rgba(0, 0, 0, 0.6); /* Dark transparent background */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
            max-width: 500px;
            text-align: center;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            font-weight: 700;
        }

        p {
            font-size: 1.2rem;
            margin-bottom: 30px;
        }

        a {
            color: #FFD700; /* Gold color for links */
            font-weight: bold;
        }

        a:hover {
            color: #FFF; /* White on hover */
            text-decoration: none;
        }

        /* Style for buttons */
        .btn {
            font-size: 1.2rem;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 600;
            transition: background-color 0.3s ease;
        }

        .btn-primary {
            background-color: #FFD700;
            border-color: #FFD700;
            color: black;
        }

        .btn-primary:hover {
            background-color: #FFC107;
            border-color: #FFC107;
            color: black;
        }
    </style>
</head>
<body>
    <div class="overlay">
        <h1>Welcome to Time Tracker</h1>
        <p>Get started by <a href="/login">Logging</a> in or <a href="/register">Registering</a>.</p>

        <a href="/login" class="btn btn-primary">Log In</a>
        <a href="/register" class="btn btn-primary ml-3">Register</a>
    </div>

    <!-- Bootstrap JS and dependencies (Optional, if needed) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
